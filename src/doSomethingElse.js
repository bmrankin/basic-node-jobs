/**
 * Use this when you want to do something else
 * @param {string} x - first param
 * @param {string} y - second param
 */
export const doSomethingElse = (x, y) => {
  return `${x} is joined to ${y} to do something else`
}
